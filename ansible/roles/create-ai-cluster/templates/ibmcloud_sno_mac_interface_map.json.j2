[
{% for interface in hostvars[groups['sno'][0]]['bond0_interfaces'] %}
    {
        "mac_address": "{{ (hostvars[groups['sno'][0]]['bond0_macs'].split(','))[loop.index0] }}",
        "logical_nic_name": "{{ interface }}"
  {% if loop.last %}
    }
  {% else %}
    },
  {% endif %}
{% endfor %}
]
