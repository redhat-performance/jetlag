. {
    errors
    health :18080
    forward . {{ lab_dns }}
    cache 30
    reload
    template IN A {{ rwn_cluster_name }}.{{ base_dns_name }} {
        match .*.apps.{{ rwn_cluster_name }}.{{ base_dns_name }}
        answer "{%raw%}{{ .Name }} 60 in {{ .Type }}{%endraw%} {{ controlplane_network_ingress }}"
        fallthrough
    }
    template IN AAAA {{ rwn_cluster_name }}.{{ base_dns_name }} {
        match .*.apps.{{ rwn_cluster_name }}.{{ base_dns_name }}
        fallthrough
    }
    template IN A {{ rwn_cluster_name }}.{{ base_dns_name }} {
        match api.{{ rwn_cluster_name }}.{{ base_dns_name }}
        answer "{%raw%}{{ .Name }} 60 in {{ .Type }}{%endraw%} {{ controlplane_network_api }}"
        fallthrough
    }
    template IN AAAA {{ rwn_cluster_name }}.{{ base_dns_name }} {
        match api.{{ rwn_cluster_name }}.{{ base_dns_name }}
        fallthrough
    }
    template IN A {{ rwn_cluster_name }}.{{ base_dns_name }} {
        match api-int.{{ rwn_cluster_name }}.{{ base_dns_name }}
        answer "{%raw%}{{ .Name }} 60 in {{ .Type }}{%endraw%} {{ controlplane_network_api }}"
        fallthrough
    }
    template IN AAAA {{ rwn_cluster_name }}.{{ base_dns_name }} {
        match api-int.{{ rwn_cluster_name }}.{{ base_dns_name }}
        fallthrough
    }
}
