---
# sync-mcgh default vars

# This will be your bastion machine (if you run setup-bastion playbook)
registry_host: "{{ groups['bastion'][0] }}"
registry_port: 5000

registry_path: /opt/jetlag/registry
sync_path: "{{ registry_path }}/sync"
